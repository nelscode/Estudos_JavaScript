/* Numbers */

let number1 = 1
let number2 = 2

console.log(number1 + number2) // somando numeros
console.log(number1.toString()) // Convertendo número para string (texto)
console.log(typeof(number2)) // Descobrindo que tipo de dado é o valor da variavel
console.log(typeof(number2).toString()) // Descobrindo que tipo de dado é o valor da variavel convertendo ele para string